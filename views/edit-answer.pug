extends layout

include utils.pug

block content
  br
  div#new-question
    span.userisasking Edit answer
      br
    form(action=`/answers/${answer.id}/edit` method='post')
      input(type='hidden' name='_csrf' value=csrfToken)
      label(for='content')
      textarea.answer-content(name='content' value=answer.content)=answer.content
      br
      button.addanswerbutton(type="submit") Submit


    //- div#new-question.postQuestion
    //- span.userisasking Edit Answer
    //- form(action=`/answers/${answer.id}/edit` method='post')
    //-   input(type='hidden' name='_csrf' value=csrfToken)
    //-   div
    //-     br
    //-     textarea.answer-content(name='content' value=answer.content)=answer.content

    //-     button.add-question(type="submit") Submit
